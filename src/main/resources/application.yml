spring:
  datasource:
    url: jdbc:mysql://localhost:3307/devmate?serverTimezone=Asia/Seoul
    username: devmate
    password: ${MYSQL_PASSWORD}
  mail:
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}

github:
  token: ${GITHUB_TOKEN}
  owner: ${GITHUB_OWNER}
  repo: ${GITHUB_REPO}

notification:
  email:
    from: ${MAIL_USERNAME}

slack:
  webhook:
    url: ${SLACK_WEBHOOK_URL}