spring:
  datasource:
    url: jdbc:mysql://localhost:3307/devmate_test?serverTimezone=Asia/Seoul
    username: devmate
    password: ${MYSQL_PASSWORD:1234}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

  data:
    redis:
      host: localhost
      port: 6379

  batch:
    job:
      enabled: false

github:
  token: test
  owner: test
  repo: test

slack:
  webhook:
    url: test

notification:
  email:
    from: test@test.com